<% form_for @questions do |f| %>
  <% f.error_messages %>
 <p>
  <%= f.label :name %>
  <%= f.text_field :name %>
 </p>
<!--  <% f.fields_for :questions do |builder| %>
  <%= render 'question_fields', :f => builder %>
 <% end %> -->
 <p><%= f.submit "submit" %></p>
<% end %>